module.exports = {
    async teste(lenguage) {
        switch (lenguage.toUpperCase()) {
            case 'JS':
                return 'logo.svg'
            case 'HTML':
                return 'logo.svg'
            default:
                return 'logo.svg'
        }
    }
};